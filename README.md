
  # Waste Management System

  This is a code bundle for Waste Management System. The original project is available at https://www.figma.com/design/a3XoSxJEKsnt0UadLfXJe5/Waste-Management-System.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  